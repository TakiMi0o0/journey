<%= render "shared/header" %>

  <ul class='schedule-lists'>
  <%= link_to(new_schedule_path, data: {"turbolinks" => false}, class: 'purchase-btn') do %>
    <h2>次の旅</h2>
  <% end %>
  <% @schedules.each do |schedule| %>
    <li class='list'> 
      <%= link_to schedule_path(schedule.id) do %>
        <div class='schedule-img-content'>
          <%#= image_tag schedule.image, class: "schedule-img" %>
        </div>

        <div class='schedule-info'>
          <h3 class='schedule-summary'>
            <%= schedule.summary %>
          </h3>
          <div class='schedule-date_time'>
            <%= schedule.date_time %>
          </div>
          <div class='schedule-icon'>
            <%= schedule.icon %>
          </div>
          <div class='schedule-location'>
            <%= schedule.location %>
          </div>
          <div class='schedule-departure'>
            <%= schedule.departure %>
          </div>
          <div class='schedule-arrival'>
            <%= schedule.arrival %>
          </div>
          <div class='schedule-departure_time'>
            <%= schedule.departure_time %>
          </div>
          <div class='schedule-arrival_time'>
            <%= schedule.arrival_time %>
          </div>
          <div class='schedule-cost'>
            <%= schedule.cost %>
          </div>
        </div>
      <% end %>
    </ul>
  <% end %>

<%= render "shared/footer" %>