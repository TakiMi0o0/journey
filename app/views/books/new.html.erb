<div class="format">
  <div class="back">
    <%= link_to image_tag('back.png'), root_path %>
  </div>

  <%= form_with model: @book, local: true do |b| %>
    <%= render "form", b: b %>

    <div class="form-set">
      <%= b.submit "出発" ,class:"submit-btn" %>
    </div>
  <% end %>
</div>