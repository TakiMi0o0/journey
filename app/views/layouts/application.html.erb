<!DOCTYPE html>
<html>
  <head>
    <title>Journey</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= javascript_importmap_tags %>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Caveat:wght@600&display=swap" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css2?family=Caveat:wght@600&family=M+PLUS+1p:wght@300&display=swap" rel="stylesheet">
  </head>

  <body>

    <div class='header-kotei'>
      <header class='header'>
        <%= link_to image_tag("journey-logo.png", class:'journey-icon-header'), "/" %>
        <form class='search-form' action="#">
          <input class='input-box' placeholder='旅を探す'>
          <button class="search-button">
            <%= image_tag "search.png", class:'search-icon' %>
          </button>
        </form>
        <div class='nav'>
          <% if user_signed_in? %>
            <%= link_to current_user.nickname, "#", class: "user-nickname" %>
            <%= link_to 'Logout', destroy_user_session_path, data: { turbo_method: :delete }, class: "logout" %>
          <% else %>
            <%= link_to 'Login', user_session_path, class: "login" %>
            <%= link_to 'Sign-up', new_user_registration_path, class: "sign-up" %>
          <% end %>
        </div>
      </header>
    </div>
    
    <%= yield %>

    <div class='footer-kotei'>
      <footer class='footer'>
        <%= link_to image_tag("journey-logo.png", class:"journey-icon-footer"), "#" %>
        <div class='footer-message'>
          LIFE IS A ©JOURNEY
        </div>
      </footer>
    </div>

  </body>
</html>
